# Project rules

- Keep the site static-first and dependency-light.
- Preserve bilingual copy and explicit public/synthetic data labels.
- Run `npm run build` and `node --check src/app.js` before publishing.
